---

- name: put --help in homedir
  copy:
    dest: "{{ homedir }}/--help"
    content: "{{ level00_flag }}"
    owner: "root"
    group: "{{ level }}"
    mode: 0440

- name: cd to homedir on login
  lineinfile:
    path: "{{ homedir }}/.bashrc"
    insertafter: EOF
    line: "cd {{ homedir }}"
